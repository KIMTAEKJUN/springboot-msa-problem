type User {
  id: ID!
  name: String!
  email: String!
  createdAt: String
}

type Query {
  user(id: ID!): User
  users: [User!]!
}

type Mutation {
  createUser(name: String!, email: String!): User!
}
